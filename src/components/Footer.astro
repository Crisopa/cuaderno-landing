---
const currentYear = new Date().getFullYear()

const footerLinks = {
  producto: [
    { label: 'Qué incluye', href: '#caracteristicas' },
    { label: 'Precios', href: '#precios' },
    { label: 'Dudas', href: '#faq' },
  ],
  legal: [
    { label: 'Términos de uso', href: '/terminos' },
    { label: 'Privacidad', href: '/privacidad' },
    { label: 'Cookies', href: '/cookies' },
  ],
  contacto: [
    { label: 'hola@appcrisopa.com', href: 'mailto:hola@appcrisopa.com' },
  ],
}
---

<footer class="relative bg-gray-950 text-gray-400 overflow-hidden">
  <!-- Subtle gradient overlay -->
  <div class="absolute inset-0 bg-gradient-to-t from-gray-950 via-gray-900/50 to-gray-950"></div>

  <!-- Top border gradient -->
  <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-gray-700 to-transparent"></div>

  <div class="relative mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-10 sm:py-16 lg:py-20">
    <div class="grid gap-8 sm:gap-12 lg:grid-cols-6">
      <!-- Brand -->
      <div class="lg:col-span-2">
        <a href="/logo" class="group flex items-center gap-2.5 w-fit">
          <div class="relative">
            <div class="absolute inset-0 bg-white/10 blur-lg rounded-full scale-0 group-hover:scale-150 transition-transform duration-500"></div>
            <svg
              class="h-9 w-9 text-white relative transition-transform duration-300 group-hover:scale-110"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M11.264 2.205A4 4 0 0 0 6.42 4.211l-4 8a4 4 0 0 0 1.359 5.117l6 4a4 4 0 0 0 4.438 0l6-4a4 4 0 0 0 1.576-4.592l-2-6a4 4 0 0 0-2.53-2.53z"/>
              <path d="M11.99 22 14 12l7.822 3.184"/>
              <path d="M14 12 8.47 2.302"/>
            </svg>
          </div>
          <span class="font-logo text-xl font-bold text-white transition-colors group-hover:text-primary-300">crisopa</span>
        </a>
        <p class="mt-5 text-sm text-gray-500 max-w-xs leading-relaxed">
          Cuaderno de explotación agrícola. Simple, legal y sin complicaciones. Hecho en España para el campo español.
        </p>

        <!-- Social hint (placeholder) -->
        <div class="mt-6 flex items-center gap-3">
          <span class="text-xs text-gray-600 uppercase tracking-wider font-medium">Síguenos</span>
          <div class="flex-1 h-px bg-gradient-to-r from-gray-700 to-transparent"></div>
        </div>
      </div>

      <!-- Links sections -->
      <div class="lg:col-span-4 grid gap-6 sm:gap-8 grid-cols-2 sm:grid-cols-3">
        <!-- Producto -->
        <div>
          <h3 class="text-sm font-semibold text-white uppercase tracking-wider flex items-center gap-2">
            <span class="w-2 h-2 rounded-full bg-gradient-to-r from-primary-400 to-primary-600"></span>
            Producto
          </h3>
          <ul class="mt-5 space-y-3">
            {footerLinks.producto.map(link => (
              <li>
                <a
                  href={link.href}
                  class="group flex items-center gap-2 text-sm transition-all duration-300 hover:text-white"
                >
                  <span class="w-0 h-px bg-primary-400 group-hover:w-3 transition-all duration-300"></span>
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>

        <!-- Legal -->
        <div>
          <h3 class="text-sm font-semibold text-white uppercase tracking-wider flex items-center gap-2">
            <span class="w-2 h-2 rounded-full bg-gradient-to-r from-accent-400 to-accent-600"></span>
            Legal
          </h3>
          <ul class="mt-5 space-y-3">
            {footerLinks.legal.map(link => (
              <li>
                <a
                  href={link.href}
                  class="group flex items-center gap-2 text-sm transition-all duration-300 hover:text-white"
                >
                  <span class="w-0 h-px bg-accent-400 group-hover:w-3 transition-all duration-300"></span>
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>

        <!-- Contacto -->
        <div>
          <h3 class="text-sm font-semibold text-white uppercase tracking-wider flex items-center gap-2">
            <span class="w-2 h-2 rounded-full bg-gradient-to-r from-gray-400 to-gray-500"></span>
            Contacto
          </h3>
          <ul class="mt-5 space-y-3">
            {footerLinks.contacto.map(link => (
              <li>
                <a
                  href={link.href}
                  class="group inline-flex items-center gap-2 text-sm transition-all duration-300 hover:text-white"
                >
                  <svg class="w-4 h-4 text-gray-600 group-hover:text-primary-400 transition-colors" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21.75 6.75v10.5a2.25 2.25 0 01-2.25 2.25h-15a2.25 2.25 0 01-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25m19.5 0v.243a2.25 2.25 0 01-1.07 1.916l-7.5 4.615a2.25 2.25 0 01-2.36 0L3.32 8.91a2.25 2.25 0 01-1.07-1.916V6.75" />
                  </svg>
                  {link.label}
                </a>
              </li>
            ))}
          </ul>

          <!-- Additional contact info -->
          <div class="mt-6 p-4 rounded-xl bg-gray-900/50 border border-gray-800/50">
            <p class="text-xs text-gray-500 leading-relaxed">
              Respondemos en menos de 24h laborables. Escríbenos para cualquier duda.
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- Bottom -->
    <div class="mt-10 sm:mt-16 pt-6 sm:pt-8 border-t border-gray-800/50">
      <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
        <p class="text-sm text-gray-600">
          &copy; {currentYear} <span class="text-gray-500 font-medium">CRISOPA</span>. Todos los derechos reservados.
        </p>
      </div>
    </div>
  </div>
</footer>
