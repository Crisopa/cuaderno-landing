---
const appUrl = 'https://app.cuaderno.es'
---

<section class="py-20 sm:py-28 bg-gradient-to-br from-primary-800 to-primary-900 relative overflow-hidden">
  <!-- Decoración de fondo -->
  <div class="absolute inset-0 -z-10">
    <div class="absolute top-0 left-1/4 w-96 h-96 bg-primary-600/30 rounded-full blur-3xl"></div>
    <div class="absolute bottom-0 right-1/4 w-64 h-64 bg-accent-500/20 rounded-full blur-3xl"></div>
  </div>

  <div class="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8 text-center">
    <p class="text-lg text-primary-200 mb-4">
      Software de gestión agrícola
    </p>
    <h2 class="text-3xl font-bold tracking-tight text-white sm:text-4xl lg:text-5xl">
      Hecho para el campo español.
    </h2>

    <div class="mt-12">
      <a
        href={`${appUrl}/register`}
        class="inline-flex items-center justify-center rounded-lg bg-white px-8 py-4 text-base font-semibold text-primary-800 shadow-lg hover:bg-primary-50 transition-all hover:shadow-xl hover:-translate-y-0.5"
      >
        Prueba gratis
        <svg class="ml-2 h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
        </svg>
      </a>
    </div>

    <!-- Trust badges -->
    <div class="mt-10 flex flex-wrap items-center justify-center gap-x-6 gap-y-3 text-sm text-primary-200">
      <span>30 días gratis</span>
      <span class="text-primary-400">·</span>
      <span>Sin tarjeta</span>
      <span class="text-primary-400">·</span>
      <span>Cancela cuando quieras</span>
    </div>
  </div>
</section>
